# activity_strain 计算原理说明

> 本文解释 `activity_strain.py` 中 `compute_activity_strain` 所体现的计算原理:
> **为什么用这条公式、每个参数代表什么、如何标定**。面向需要理解或调参的使用者。

---

## 1. 它解决什么问题(定位)

主 strain 是**心血管强度负荷**:由白天逐分钟心率经 HRR → TRIMP → 饱和映射算出。它的盲区是**日常零散走动**——上班路上走两步、楼里转一圈,心率根本不上去,在强度模型里贡献几乎为零。但从"综合活动负荷"的角度,这些步数代表了真实的日常活动量,应当被体现。

`activity_strain` 就是补这个盲区的**独立活动量通道**:它不看强度,只看"走了多少步",把当日累计步数映射成一个有界的活动量分,再在外层与心率 strain 合成。

**关键定位**:它衡量的是**活动"量"**,不是活动"强度"——这正是零散走动的价值所在(走了多少、动了多久),也是强度模型不擅长的维度。

---

## 2. 核心公式

```
strain_activity = ACTIVITY_CAP · (1 − e^(−cumulative_steps / TAU_STEP))
```

- `cumulative_steps`:上游已按**低心率门控**累加好的当日累计步数。
- `ACTIVITY_CAP = 8.0`:活动量分的上限。
- `TAU_STEP ≈ 5770.78`:控制曲线快慢的时间常数(由标定反解,见 §5)。

输出落在 `[0, ACTIVITY_CAP]`,即 `[0, 8]`。

---

## 3. 为什么是"饱和指数"曲线

这条曲线的形状不是随意选的,它同时满足四个对"活动量"语义至关重要的性质:

**① 有界(渐近封顶)。** 当步数 → ∞,`e^(−steps/τ) → 0`,分数渐近于 `ACTIVITY_CAP` 但永不超过。这保证"纯走路"无论走多少都封在 8 分以内——**高于 8 的 strain 只能靠真实强度(心率)挣得**,守住了"综合负荷但强度仍主导高端"的边界。

**② 单调递增。** 累计步数只增不减,分数随之单调上升。符合"日内累计活动量"的语义——走得越多,分越高(只是越来越慢)。

**③ 边际递减(符合直觉)。** 同样多走 2000 步,在不同起点价值不同:

| 区间 | 增加的分数 |
|---|---|
| 0 → 2000 步 | +2.34 |
| 10000 → 12000 步 | +0.41 |

从久坐到开始走,身体多付出明显(边际高);从一万走到一万二,边际意义很小。饱和曲线**自动**给出这种递减,线性映射做不到。

**④ 与心率 strain 同构,便于合成。** 心率 strain 用的是 `21·(1 − e^(−trimp/τ))`,同一个"饱和指数"家族。活动量通道用同样的骨架,两条曲线"手感"一致(都是前段灵敏、越高越难涨、有上限),在同一个 0–21 世界里行为协调,取大合成时不会突兀。

---

## 4. 两个参数的物理含义

**`ACTIVITY_CAP`(上限 / 渐近线)** —— 纯走路能挣到的 strain 天花板。它回答"日常活动最多值多少分"。设为 **8**,意味着把"日常活动量"钉在中低位,把 8 分以上的高 strain 完全留给心率能证明的真实强度。

**`TAU_STEP`(尺度 / 快慢)** —— 控制曲线多快接近上限。它有一个非常直观的含义:

> **当 `cumulative_steps = TAU_STEP` 时,分数恰好达到 `ACTIVITY_CAP` 的 63.2%。**

因为 `1 − e^(−1) ≈ 0.632`。所以 `TAU_STEP` 就是"走到 63% 封顶分所需的步数"。`TAU_STEP` 越小,曲线越陡、越快饱和(日常步数更快有存在感);越大则越平缓。

---

## 5. 标定原理:`TAU_STEP` 怎么来的

`TAU_STEP` **不是拍脑袋的魔数**,而是由一个业务锚点反解出来的。锚点是:

> **8000 步 → 6.0 分**(`ANCHOR_STEPS = 8000`,`ANCHOR_SCORE = 6.0`)

把锚点代入公式反解 `TAU_STEP`:

```
ANCHOR_SCORE = ACTIVITY_CAP · (1 − e^(−ANCHOR_STEPS / TAU_STEP))
        6    = 8 · (1 − e^(−8000 / TAU_STEP))
       6/8   = 1 − e^(−8000 / TAU_STEP)
   e^(−8000/TAU_STEP) = 1 − 0.75 = 0.25
      −8000/TAU_STEP  = ln(0.25)
        TAU_STEP = −8000 / ln(0.25) ≈ 5770.78
```

代码里就是这一行:
```python
TAU_STEP = -ANCHOR_STEPS / math.log(1.0 - ANCHOR_SCORE / ACTIVITY_CAP)
```

**通用反解公式**(想让"`S` 步 → `y` 分"):
```
TAU_STEP = − S / ln(1 − y / ACTIVITY_CAP)
```

**如何重新标定**:只改 `ANCHOR_STEPS / ANCHOR_SCORE / ACTIVITY_CAP` 三个常量,`TAU_STEP` 在模块加载时自动重算,整条曲线随之平移。例如想让日常步数更快积累分数,把锚点改成"6000 步 → 6 分"即可。**真实标定应基于你的用户群**:看日常步数中位数、决定"中位活跃日"该得几分,反解出 `TAU_STEP`。

---

## 6. 曲线行为(当前配置 CAP=8, TAU_STEP≈5771)

| 累计步数 | 活动量分 |
|---|---|
| 2000 | 2.34 |
| 4000 | 4.00 |
| 6000 | 5.17 |
| 8000 | **6.00**(锚点) |
| 10000 | 6.59 |
| 12000 | 7.00 |
| 15000 | 7.41 |
| 20000 | 7.75 |
| → ∞ | → 8.00(封顶) |

读法:走到 4000 步拿 4 分,8000 步(典型活跃日)拿 6 分,之后边际递减、缓慢逼近 8,**永不超过 8**。

---