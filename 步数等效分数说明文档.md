# activity_strain 计算原理说明

> 本文解释 `activity_strain.py` 中 `compute_activity_strain` 所体现的计算原理:
> **为什么用这条公式、每个参数代表什么、如何标定**。面向需要理解或调参的使用者。

---

## 1. 它解决什么问题(定位)

主 strain 是**心血管强度负荷**:由白天逐分钟心率经 HRR → TRIMP → 饱和映射算出。它的盲区是**日常零散走动**——上班路上走两步、楼里转一圈,心率根本不上去,在强度模型里贡献几乎为零。但从"综合活动负荷"的角度,这些步数代表了真实的日常活动量,应当被体现。

`activity_strain` 就是补这个盲区的**独立活动量通道**:它不看强度,只看"走了多少步",把当日累计步数映射成一个有界的活动量分,再在外层与心率 strain 合成。

**关键定位**:它衡量的是**活动"量"**,不是活动"强度"——这正是零散走动的价值所在(走了多少、动了多久),也是强度模型不擅长的维度。

---

## 2. 核心公式

```
strain_activity = ACTIVITY_CAP · (1 − e^(−cumulative_steps / TAU_STEP))
```

- `cumulative_steps`:上游已按**低心率门控**累加好的当日累计步数。
- `ACTIVITY_CAP = 8.0`:活动量分的上限。
- `TAU_STEP ≈ 2885.39`:控制曲线快慢的时间常数(由标定反解,见 §5)。

输出落在 `[0, ACTIVITY_CAP]`,即 `[0, 8]`。

---

## 3. 为什么是"饱和指数"曲线

这条曲线的形状不是随意选的,它同时满足四个对"活动量"语义至关重要的性质:

**① 有界(渐近封顶)。** 当步数 → ∞,`e^(−steps/τ) → 0`,分数渐近于 `ACTIVITY_CAP` 但永不超过。这保证"纯走路"无论走多少都封在 8 分以内——**高于 8 的 strain 只能靠真实强度(心率)挣得**,守住了"综合负荷但强度仍主导高端"的边界。

**② 单调递增。** 累计步数只增不减,分数随之单调上升。符合"日内累计活动量"的语义——走得越多,分越高(只是越来越慢)。

**③ 边际递减(符合直觉)。** 同样多走 2000 步,在不同起点价值不同:

| 区间 | 增加的分数 |
|---|---|
| 0 → 2000 步 | +4.00 |
| 10000 → 12000 步 | +0.13 |

从久坐到开始走,身体多付出明显(边际高);从一万走到一万二,边际意义很小。饱和曲线**自动**给出这种递减,线性映射做不到。

**④ 与心率 strain 同构,便于合成。** 心率 strain 用的是 `21·(1 − e^(−trimp/τ))`,同一个"饱和指数"家族。活动量通道用同样的骨架,两条曲线"手感"一致(都是前段灵敏、越高越难涨、有上限),在同一个 0–21 世界里行为协调,取大合成时不会突兀。

---

## 4. 两个参数的物理含义

**`ACTIVITY_CAP`(上限 / 渐近线)** —— 纯走路能挣到的 strain 天花板。它回答"日常活动最多值多少分"。设为 **8**,意味着把"日常活动量"钉在中低位,把 8 分以上的高 strain 完全留给心率能证明的真实强度。

**`TAU_STEP`(尺度 / 快慢)** —— 控制曲线多快接近上限。它有一个非常直观的含义:

> **当 `cumulative_steps = TAU_STEP` 时,分数恰好达到 `ACTIVITY_CAP` 的 63.2%。**

因为 `1 − e^(−1) ≈ 0.632`。所以 `TAU_STEP` 就是"走到 63% 封顶分所需的步数"。`TAU_STEP` 越小,曲线越陡、越快饱和(日常步数更快有存在感);越大则越平缓。

---

## 5. 标定原理:`TAU_STEP` 怎么来的

`TAU_STEP` **不是拍脑袋的魔数**,而是由一个业务锚点反解出来的。锚点是:

> **4000 步 → 6.0 分**(`ANCHOR_STEPS = 4000`,`ANCHOR_SCORE = 6.0`)

把锚点代入公式反解 `TAU_STEP`:

```
ANCHOR_SCORE = ACTIVITY_CAP · (1 − e^(−ANCHOR_STEPS / TAU_STEP))
        6    = 8 · (1 − e^(−4000 / TAU_STEP))
       6/8   = 1 − e^(−4000 / TAU_STEP)
   e^(−4000/TAU_STEP) = 1 − 0.75 = 0.25
      −4000/TAU_STEP  = ln(0.25)
        TAU_STEP = −4000 / ln(0.25) ≈ 2885.39
```

代码里就是这一行:
```python
TAU_STEP = -ANCHOR_STEPS / math.log(1.0 - ANCHOR_SCORE / ACTIVITY_CAP)
```

**通用反解公式**(想让"`S` 步 → `y` 分"):
```
TAU_STEP = − S / ln(1 − y / ACTIVITY_CAP)
```

**如何重新标定**:只改 `ANCHOR_STEPS / ANCHOR_SCORE / ACTIVITY_CAP` 三个常量,`TAU_STEP` 在模块加载时自动重算,整条曲线随之平移。例如想让日常步数更快积累分数,把锚点改成"3000 步 → 6 分"即可。**真实标定应基于你的用户群**:看日常步数中位数、决定"中位活跃日"该得几分,反解出 `TAU_STEP`。

---

## 6. 曲线行为(当前配置 CAP=8, TAU_STEP≈2885)

| 累计步数 | 活动量分 |
|---|---|
| 2000 | 4.00 |
| 4000 | **6.00**(锚点) |
| 6000 | 7.00 |
| 8000 | 7.50 |
| 10000 | 7.75 |
| 12000 | 7.88 |
| 15000 | 7.96 |
| 20000 | 7.99 |
| → ∞ | → 8.00(封顶) |

读法:走到 2000 步拿 4 分,4000 步(约半个典型活跃日)拿 6 分,之后边际递减、缓慢逼近 8,**永不超过 8**。

---

## 7. 如何与心率 strain 合成:`max`,且不双计

```
strain_final = max(strain_hr, strain_activity)
```

**为什么用取大,而不是相加或加权:**

- **不双计**:走路时的心率其实已经进了 `strain_hr`。如果再把步数通过活动量"加"一遍,跑步/快走会被算两次而虚高。取大则只认两者中更突出的那个,天然规避双计。
- **不被零步数拖累**:骑行、划船这类高心率、零步数的运动,`strain_activity = 0`,取大后仍由 `strain_hr` 主导——若用加权或相加,这些日子会被零活动量稀释。
- **语义清晰**:这一天的 strain = "要么你强度够高(心率证明),要么你走得够多(步数证明),哪个更突出算哪个"。

**为什么不会双计——更深一层**:本通道的输入是上游**已按低心率门控**累加的步数,即"跑步那一小时的步数从源头就被排除了"。所以活动量通道里压根不含高心率时段的步数,与 `strain_hr` 在来源上就不重叠。`max` 只是末端兜底,双计在源头已被堵死。

> 取整提示:`strain_hr` 已是一位小数,本函数返回精确浮点。建议对最终的 `max(...)` 结果套用现有的一位小数取整,避免双重取整。

---

## 8. 输入契约(务必遵守)

`cumulative_steps` 必须是**上游已按低心率门控累加好的当日累计步数**。本模块:

- **不做门控**:它假设高心率时段的步数已在上游被排除。若误传"设备原始当日总步数",低心率门控原则会被绕过(跑步步数混入活动量)。
- **不做异常过滤**:之前讨论的单小时异常剔除(如某小时报 5 万步)应放在上游累加之前。本函数只保留最低防御——`None` / 负数 / 非数字 / `NaN` / `Inf` → 返回 `0.0`。

---

## 9. 设计边界(它刻意不做什么)

- **只认"量",不认"分布"**:2000 步无论怎么分布在一天里、集中还是零散,结果一样。零散走动的价值本就在量,这是特性不是缺陷。
- **只认"量",不认"强度"**:它不试图区分慢走/快走/跑(那是心率 strain 的职责)。这也是为什么用纯步数总量,而非"步频→强度"那套——后者对零散走动会失效(零散走动平均步频太低,会被判成零负荷)。
- **天花板低于满量程**:`ACTIVITY_CAP=8 ≪ 21`。这是有意的——"走了两万步轻松散步"不该和"跑了一场马拉松"同档。真正的高 strain 永远需要真实强度来证明。

---

## 附:一句话原理

**把"上游低心率门控后的累计步数"喂进一条与心率 strain 同族的有界饱和曲线 `8·(1−e^(−步数/τ))`;`τ` 由"4000步→6分"反解而来;输出封顶 8 分,与心率 strain 取大合成——从而在不破坏强度语义、不双计的前提下,让日常零散走动在 0–21 的综合负荷里有了确定且有界的体现。**